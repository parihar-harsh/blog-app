{
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "npm run typecheck && wrangler deploy --dry-run --outdir dist src/index.ts",
    "deploy": "npm run typecheck && wrangler deploy --minify src/index.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.10.2",
    "@prisma/extension-accelerate": "^0.6.3",
    "bcryptjs": "^3.0.2",
    "hono": "^4.0.8",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "prisma": "^5.10.2",
    "typescript": "^5.3.3",
    "vitest": "^2.1.9",
    "wrangler": "^4.105.0"
  }
}
